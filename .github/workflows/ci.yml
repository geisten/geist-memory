name: C23 library
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  linux:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-24.04, ubuntu-24.04-arm]
        cc: [gcc-14, clang-19]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: geisten/geistlib
          ref: 32b432660948a50be05b355efa74a789456a37dd
          path: build/geistlib
      - run: sudo apt-get update && sudo apt-get install -y gcc-14 g++-14 clang-19
      - run: make -j2 check check-linkage check-install example model-tools import-tool check-package
        env:
          CC: ${{ matrix.cc }}
          GEISTLIB: build/geistlib
      - run: make -j2 MODE=asan check fuzz test-model-alloc test-tokenizer-oom
        env:
          CC: ${{ matrix.cc }}
          GEISTLIB: build/geistlib
      - name: Real ENOSPC on a disposable filesystem
        run: |
          sudo mkdir /mnt/gm-enospc
          sudo mount -t tmpfs -o size=32m tmpfs /mnt/gm-enospc
          sudo chown "$USER" /mnt/gm-enospc
          trap 'sudo umount /mnt/gm-enospc' EXIT
          make MODE=asan test-enospc ENOSPC_DIR=/mnt/gm-enospc
        env:
          CC: ${{ matrix.cc }}
      - if: matrix.cc == 'clang-19'
        run: make CC=clang-19 analyze fuzz-libfuzzer FUZZ_CC=clang-19 FUZZ_SECONDS=30
  macos:
    strategy:
      fail-fast: false
      matrix:
        runner: [macos-15, macos-15-intel]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: geisten/geistlib
          ref: 32b432660948a50be05b355efa74a789456a37dd
          path: build/geistlib
      - run: brew install llvm
      - run: echo "$(brew --prefix llvm)/bin" >> "$GITHUB_PATH"
      - run: make -j2 CC=clang check check-linkage check-install example model-tools import-tool check-package
        env:
          GEISTLIB: build/geistlib
      - run: make -j2 CC=clang MODE=asan check fuzz
      - run: make CC=clang analyze format-check
      - run: make -j2 CC=clang check-repro
        env:
          GEISTLIB: build/geistlib
  musl-static:
    strategy:
      fail-fast: false
      matrix:
        runner: [ubuntu-24.04, ubuntu-24.04-arm]
    runs-on: ${{ matrix.runner }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/checkout@v4
        with:
          repository: geisten/geistlib
          ref: 32b432660948a50be05b355efa74a789456a37dd
          path: build/geistlib
      - run: >-
          docker run --rm --tmpfs /enospc:size=32m -v "$PWD:/work" -w /work alpine:3.22
          sh -ec 'apk add --no-cache build-base clang git binutils tar gzip linux-headers;
          git config --global --add safe.directory /work/build/geistlib;
          make -j2 CC=clang LINK=static GEISTLIB=build/geistlib
          check check-linkage check-install example model-tools import-tool check-package test-enospc ENOSPC_DIR=/enospc'
